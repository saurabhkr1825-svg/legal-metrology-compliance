name: slm_mobile
description: Smart Legal Metrology — Field Officer App
version: 0.1.0+1
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  uuid: ^4.4.0
  http: ^1.2.0
  camera: ^0.11.0
  image_picker: ^1.1.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  connectivity_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
